Starting development
====================

After following the steps in the user :ref:`Installation` steps, the development dependencies also need to be installed:

    1. They are contained in deps/dev-requirements.pip. Install with :code:`pip install -r deps/dev-requirements.pip`
