name: mantidimaging
channels:
  - mantid
  - dtasev
  - astra-toolbox/label/dev
  - conda-forge
  - defaults
dependencies:
  - mantidimaging
  - pip
  - sharedarray
  - sarepy
  - astra-toolbox
  - pip:
      - pytest
      - pytest-cov
      - coveralls
      - pyqt5==5.15
      - pyqtgraph==0.11
      - yapf
      - mypy
      - flake8
      - testfixtures
      - coverage
      - hazelnut
      - gitpython
      - mock
      - pylint
      - sphinx
      - sphinx-multiversion
      - pytest-randomly
      - pytest-parallel
      - psutil
      - isort
