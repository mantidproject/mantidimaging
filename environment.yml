name: mantidimaging
channels:
  - mantid
  - dtasev
  - astra-toolbox/label/dev
  - conda-forge
  - defaults
dependencies:
  - mantidimaging
  - pip
  - sharedarray
  - pip:
      - pytest
      - pytest-xvfb
      - pyqt5
      - yapf
      - mypy
      - flake8
prefix: /home/sam/miniconda3/envs/mantidimaging
