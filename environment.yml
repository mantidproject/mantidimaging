name: mantidimaging
channels:
  - mantid
  - dtasev
  - astra-toolbox/label/dev
  - conda-forge
  - defaults
dependencies:
  - mantidimaging
  - pip
  - sharedarray
  - sarepy
  - astra-toolbox
  - pip:
      - pytest
      - pyqt5
      - yapf
      - mypy
      - flake8
      - testfixtures
      - coverage
      - hazelnut
      - gitpython
      - mock
      - pylint
      - sphinx
      - pytest-randomly
      - pytest-parallel
      - pytest-qt
      - isort
prefix: /home/sam/miniconda3/envs/mantidimaging
